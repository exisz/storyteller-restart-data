# 05 Integration — Act Two Threshold Packet

Rollup command:

```bash
gameplay design rollup --source restart
```

Result:
- cards: 13 files -> 13 items -> `public/data/restartCards.json5`
- rites: 17 files -> 17 items -> `public/data/restartRite+nodes.json5`
- events: 10 files -> 10 items -> `public/data/restartEvent+nodes.json5`

Public data repo sync:

```bash
node scripts/sync_restart_data_repo.mjs --push
```

Result: pushed data repo commit `0ced8a0 Update RESTART data 1c5dd7f`.

Canonical save catalog `restart-main` was updated only to include the new cards/rites/events and the revised rite 13 unlock edge; the save was not advanced.
