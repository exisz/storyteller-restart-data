# Integration — public witness escort packet

Rollup: `gameplay design rollup --source restart` succeeded.

Counts after rollup:
- cards: 53 files -> 53 items -> `public/data/restartCards.json5`
- rites: 56 files -> 56 items -> `public/data/restartRite+nodes.json5`
- events: 25 files -> 25 items -> `public/data/restartEvent+nodes.json5`

Public data repo sync: `node scripts/sync_restart_data_repo.mjs --push` succeeded and pushed data commit `4fa4fdb` (`Update RESTART data 0fb2b2a`).

Graph disconnects expected: none for the new packet; it enters via `restart_next_region_public_witness_escort_route` and `出院活证人账页` from rite 50.
