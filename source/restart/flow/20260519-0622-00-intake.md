# 00 Intake — Act Two Threshold Packet

Current save is turn 11 with unresolved `restart-event-11-bed-number-broadcast`. The hand has Linxi, radio, seventh-sun ticket, Yue, eighth-loop wristband, and memory ampoule. The existing graph already covers the signature-room chain through `restart-rite-13-false-exit-audit`, so the blocker is not immediate playability but the next horizon after the audit.

Active region/thread: the first exit attempt is becoming the Act Two threshold. The relevant pressures are paperwork credibility, whether Linxi is officially dead enough to leave, Yue's debt, the radio/broadcast as an adversarial system, and the Seventh Sunrise shifting from mystery to witness.

Do not touch the already-played turn 1-10 path or advance `restart-main` as Player. Keep the current route choices viable: Linxi kept memory, Yue was saved, and the audit path assumes the Linxi decoy-death route.
