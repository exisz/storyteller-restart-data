# Architecture — discharge enforcement

## Region

Ward Zero discharge-enforcement counters: the place where a hearing writ must be converted into an actual exit action.

## Main pressure

The hospital accepts the hearing happened but tries to reduce the writ to a temporary paper that cannot open the exit. The player must convert public minutes, oath evidence, and supply-chain contradictions into enforceable exit-state.

## Side pressures

- Review clerks try to place Seventh Sunrise back on a复核名单 before the writ reaches the exit gate.
- Broadcast/static tries to make the public record inaudible at the door.
- Linxi can either protect Seventh Sunrise openly or use B-07/0 as a sheltering shell.

## Player choices

- Present the writ as Seventh Sunrise's public right vs shelter it in Linxi's B-07/0 contradiction.
- Force public enforcement vs smuggle enforcement through Ward Zero forms.
- Preserve witness names at the door vs spend supply-chain proof to satisfy the exit gate.

## Required future memory

Branch flags should later distinguish a public exit-right route from a protected/sheltered exit-right route, and whether the final door owes its authority to public minutes, supply-chain proof, or broadcast debt.

## Topology

Five connected rites open from the existing `restart_next_region_discharge_enforcement_route` / `restart_next_region_bed_zero_writ_shelter_route` flags and consume the current hand. Two pressure events are condition-driven with explicit `NOT:...answered` guards. The packet ends by opening a post-exit pursuit/escalation route rather than declaring final escape.
