# 00 Intake — Turn 31 return-to-hospital blocker

Current save is `restart-main`, turn 31. The hand already contains the full paper-factory/supply-chain toolset: Linxi, Seventh Sunrise, tower operator, living file, worker testimony, supply ledger, quota clerk, living paper lot, return invoice, night-shift ticket, watermark mold, and uncut certificate paper.

## Blocked / low quality

`gameplay check-playable` reports no unresolved event and no available rite can be filled by the current hand. The last completed content is `restart-rite-30-sealed-return-tram`, which sets `restart_sealed_return_tram_departed`, `restart_return_tram_public_account_route`, and `restart_next_region_hospital_supply_reversal_route`. The story has delivered the active characters and evidence back toward old Sunrise Hospital, but there is no receiving-dock content to make that return playable.

## Active region / thread

Active region: old Sunrise Hospital, seen from the supply-chain receiving side rather than as a patient ward.

Threads to advance:
- Paperwork/supply-chain: living paper and supply ledger return to the institution that consumed pre-written death quota.
- Seventh Sunrise witness: she returns as public living witness rather than hidden patient.
- Broadcast/radio/operator: the tower voice is now implicated in hospital intake announcements.
- Linxi identity: the original B-07/0 bed record can be reversed using supply evidence.

## Do not touch

Do not advance `restart-main` as Player. Do not rewrite earlier route choices. Do not remove existing strict conditions on turns 26–30; this packet should consume the public return route produced by the current save.
