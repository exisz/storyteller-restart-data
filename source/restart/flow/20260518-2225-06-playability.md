# 06 Playability

Current canonical save remains turn 9 and playable via `RESTART 09：救护车月台`.

Depth gate: `node scripts/restart_depth_check.mjs` returned ACCEPT.

Temporary copy validation:
- `gameplay continue --days 3` from turn 9 reached turn 12 with unresolved event `第十二回合事件：签名栏催告`, so the next Player can resolve a meaningful choice rather than hitting empty content.
- `gameplay continue --days 4` reached turn 13 with fillable rite `RESTART 13：假出口审计`, proving the packet continues beyond the three-day target.

Meaningful pressures now visible:
- memory vs Yue access (`restart_saved_linxi_memory_route` / `restart_preserved_yue_access_route`);
- proof stabilization vs broadcast erasure (`restart_linxi_memorized_bed_zero` / `restart_yue_stamped_bed_zero`);
- rescue promise vs Yue debt truth (`restart_promised_rescue_seventh_sun` / `restart_demanded_yue_debt_truth`);
- decoy death vs debt death (`restart_signed_linxi_decoy_death` / `restart_signed_yue_debt_death`).
