# Branch Design — Supply Ledger Account House

Rite 26: enter account house. Requires decoded coordinate plus current paper proof. Grants `供纸账本` and `配额账房 clerk`. Choice files worker testimony as public evidence (`restart_worker_testimony_entered_as_account_evidence`) or hides the stolen watermark mold in the ledger spine (`restart_watermark_mold_hidden_in_supply_ledger`).

Event 27: quota bell. Player chooses witness protection (`restart_quota_bell_protected_worker_names`) or shipment contamination (`restart_quota_bell_marked_batch_alive`).

Rite 27: allocation clerk confrontation. Requires the ledger and clerk plus either event pressure. Grants `死亡配额欠条`. Choice freezes the next hospital death quota or redirects it to a blank lot.

Rite 28: living-paper lot. Requires quota debt note plus uncut paper / living file. Grants `活纸批次`. Choice binds the lot to Seventh Sunrise witness route or to Linxi's decoy-death paperwork.

Event 29: return shipment alarm. Choice uses radio/operator debt or audit stamp/recheck paperwork to hide the lot.

Rite 29: hospital return invoice. Requires living lot and quota ledger. Grants `回院供货单`. Choice opens public indictment route or covert return route.

Rite 30: sealed return tram. Requires invoice plus night ticket / map. Opens next region by putting the controlled paper shipment back toward old Sunrise Hospital.
